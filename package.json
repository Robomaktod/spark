{
  "name": "spark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spark \u2014 a zero-player programming game where bots author physics-priced spells",
  "workspaces": [
    "packages/protocol",
    "packages/engine",
    "packages/sdk-ts",
    "apps/cli",
    "apps/viewer-ascii",
    "bots/naive",
    "bots/positional"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b",
    "test": "npm run build && node --test packages/engine/dist/test/*.test.js apps/cli/dist/src/*.test.js",
    "match": "npm run build && node apps/cli/dist/src/main.js run",
    "demo": "npm run build && node apps/cli/dist/src/main.js run --seed 7 --replay replays/demo.json && node apps/viewer-ascii/dist/src/main.js replays/demo.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
